Helper methods for tumeke python

## Installation

1. Run `brew install yq` to get yaml parser
2. Run `./set_symlink.sh` to configure git hooks
	- We have a git hook running that compiles latest 
	yaml and makes sure the python libs get the correct json